
from app.endpoints import curve
from app.endpoints import status
from app.endpoints import position
from app.endpoints import bridge
from app.endpoints import light
from app.endpoints import group
from app.endpoints import webhook
from app.endpoints import settings

__all__ = [
    curve,
    status,
    position,
    bridge,
    light,
    group,
    webhook,
    settings
]
